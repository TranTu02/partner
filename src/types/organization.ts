export interface Organization {
    organizationName: string;
    address: string;
    email: string;
    phone: string;
    taxId: string;
}

export interface Branch {
    branchName: string;
    address: string;
    email: string;
    phone: string;
}
